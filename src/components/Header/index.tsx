import Navbar from "components/Navbar";
import {SHeader} from "./Header.styles.ts";

const Header = () => {
    return (
        <SHeader>
          <Navbar />
        </SHeader>
    );
};

export default Header;